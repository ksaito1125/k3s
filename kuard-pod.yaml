apiVersion: v1
kind: Pod
metadata:
  name: kuard
spec:
  containers:
    - image: ksaito1125/kuard
      name: kuard
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
